# pkdir
something i can run on windows and linux to view my directories with the same output style
its easy to compile and setup

compile it using this
```
bun build pkdir.js --compile --outfile pkdir
```
